# Luma CMS v0.0.25-rc.9 — Tagged shared-hosting release

Tagged build for stage testing. Includes all installer fixes since rc.5.

## Highlights

- Shared zip: writable `storage` + `bootstrap/cache` (0777) via `scripts/ensure-writable-paths.sh`
- Setup wizard: light/dark theme, requirements fix, version display, pointer cursor on buttons
- Root `index.php` nginx-safe install flow

Download: `luma-cms-0.0.25-rc.9-shared.zip`

Git tag: `v0.0.25-rc.9`
